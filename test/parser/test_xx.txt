global_var = 10

def greet(name, age) do
  message = "Hello, " + name + "! You are " + age + " years old."
  puts(message)
  temp_var = 50 # Variável local
end

puts("Global var: " + global_var) # Deve funcionar
puts(message)
puts(name)